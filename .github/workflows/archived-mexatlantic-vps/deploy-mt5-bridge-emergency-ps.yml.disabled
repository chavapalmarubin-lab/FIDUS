name: MT5 Bridge Emergency Restart (HTTP API) [DISABLED]

# DISABLED: Feb 24, 2026 - LUCRUM-ONLY Mode
# This workflow targeted the old MEXAtlantic VPS (92.118.45.135) which is no longer used.
# LUCRUM accounts are now synced via the "Sync LUCRUM Accounts to MongoDB" workflow.

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is DISABLED - use Sync LUCRUM Accounts instead'
        required: false
        default: 'DISABLED - LUCRUM-ONLY MODE'

jobs:
  disabled-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled Notice
        run: |
          echo "⚠️ THIS WORKFLOW IS DISABLED"
          echo "=============================="
          echo ""
          echo "REASON: FIDUS is now in LUCRUM-ONLY mode."
          echo "The old MEXAtlantic VPS (92.118.45.135) is no longer used."
          echo ""
          echo "For LUCRUM account syncing, use:"
          echo "  → 'Sync LUCRUM Accounts to MongoDB' workflow"
          echo ""
          echo "This workflow has been disabled to stop unnecessary failure emails."
          exit 0
