name: Protect Critical Documentation Files

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  schedule:
    # Run every hour to check file existence
    - cron: '0 * * * *'

jobs:
  check-system-master:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Full history

      - name: Check if critical files exist
        id: check-files
        run: |
          MISSING_FILES=""
          
          if [ ! -f "SYSTEM_MASTER.md" ]; then
            echo "❌ CRITICAL: SYSTEM_MASTER.md is MISSING!"
            MISSING_FILES="SYSTEM_MASTER.md "
          else
            echo "✅ SYSTEM_MASTER.md exists"
          fi
          
          if [ ! -f "DATABASE_FIELD_STANDARDS.md" ]; then
            echo "❌ CRITICAL: DATABASE_FIELD_STANDARDS.md is MISSING!"
            MISSING_FILES="${MISSING_FILES}DATABASE_FIELD_STANDARDS.md"
          else
            echo "✅ DATABASE_FIELD_STANDARDS.md exists"
          fi
          
          if [ -n "$MISSING_FILES" ]; then
            echo "files_missing=true" >> $GITHUB_OUTPUT
            echo "missing_list=$MISSING_FILES" >> $GITHUB_OUTPUT
          else
            echo "files_missing=false" >> $GITHUB_OUTPUT
          fi

      - name: Check if files were deleted in this commit
        if: steps.check-files.outputs.files_missing == 'true'
        run: |
          echo "Checking last commit..."
          git log -1 --name-status
          echo ""
          echo "🚨 CRITICAL FILES MISSING: ${{ steps.check-files.outputs.missing_list }}"
          echo "This is a CRITICAL violation."
          exit 1

      - name: Attempt to restore files if deleted
        if: steps.check-files.outputs.files_missing == 'true'
        continue-on-error: true
        run: |
          echo "Attempting to restore critical files from history..."
          RESTORED=""
          
          # Try to restore SYSTEM_MASTER.md
          if [ ! -f "SYSTEM_MASTER.md" ]; then
            if git checkout HEAD~1 -- SYSTEM_MASTER.md 2>/dev/null; then
              echo "✅ SYSTEM_MASTER.md restored from previous commit"
              RESTORED="${RESTORED}SYSTEM_MASTER.md "
            fi
          fi
          
          # Try to restore DATABASE_FIELD_STANDARDS.md
          if [ ! -f "DATABASE_FIELD_STANDARDS.md" ]; then
            if git checkout HEAD~1 -- DATABASE_FIELD_STANDARDS.md 2>/dev/null; then
              echo "✅ DATABASE_FIELD_STANDARDS.md restored from previous commit"
              RESTORED="${RESTORED}DATABASE_FIELD_STANDARDS.md"
            fi
          fi
          
          if [ -n "$RESTORED" ]; then
            git config user.name "GitHub Action - File Protection"
            git config user.email "action@github.com"
            git add SYSTEM_MASTER.md DATABASE_FIELD_STANDARDS.md
            git commit -m "🚨 AUTO-RESTORE: Critical files restored - $RESTORED"
            git push
          else
            echo "❌ Could not restore files - they may have never existed in git history"
          fi

      - name: Create issue if file missing
        if: steps.check-file.outputs.file_exists == 'false'
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚨 CRITICAL: SYSTEM_MASTER.md is missing',
              body: `## Critical File Missing
              
              **File:** SYSTEM_MASTER.md
              **Status:** MISSING
              **Commit:** ${{ github.sha }}
              **Time:** ${new Date().toISOString()}
              
              ### Action Required:
              1. Create SYSTEM_MASTER.md with proper documentation
              2. Commit and push the file
              3. Verify file protection is working
              
              ### Context:
              The codebase references this file in multiple locations but it does not exist in the repository.
              
              **Referenced in:**
              - backend/validation/field_registry.py
              - backend/server.py (Section 4.1)
              
              This file is CRITICAL for system operation and must be restored immediately.`,
              labels: ['critical', 'documentation', 'bug']
            })
