name: Monitor MT5 Bridge Health [DISABLED]

# DISABLED: Feb 24, 2026 - LUCRUM-ONLY Mode
# This workflow targeted the old MEXAtlantic VPS (92.118.45.135) which is no longer used.
# LUCRUM accounts are synced via GitHub Actions directly to MongoDB.

on:
  # schedule:
  #   - cron: '*/15 * * * *'  # Disabled - was every 15 minutes
  workflow_dispatch:  # Manual trigger only

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled Notice
        run: |
          echo "⚠️ THIS WORKFLOW IS DISABLED"
          echo "=============================="
          echo ""
          echo "REASON: FIDUS is now in LUCRUM-ONLY mode."
          echo "The old MEXAtlantic VPS (92.118.45.135) is no longer active."
          echo ""
          echo "For LUCRUM account management, use:"
          echo "  → 'Sync LUCRUM Accounts to MongoDB' workflow"
          echo ""
          exit 0
